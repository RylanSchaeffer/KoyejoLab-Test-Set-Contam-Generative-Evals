
#
*$py.class
.abstra/
# Abstra
# Abstra is an AI-powered process automation framework.
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
# a file underneath them -- the surviving pretraining caches in 10/ and 11/ are force-added
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  and can be added to the global gitignore or merged into this file. However, if you prefer, 
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
build/
# Byte-compiled / optimized / DLL files
.cache
/cached_datasets/
celerybeat.pid
celerybeat-schedule
# Celery stuff
# C extensions
.claude/
# Claude Code IDE settings
#   commonly ignored for libraries.
*.cover
cover/
.coverage
.coverage.*
coverage.xml
cython_debug/
# Cython debug symbols
db.sqlite3
db.sqlite3-journal
develop-eggs/
dist/
# Distribution / packaging
# Django stuff:
.dmypy.json
dmypy.json
docs/_build/
# Downloaded data. NOTE: these exclude the whole directory, so a `!` negation cannot re-include
downloads/
*.egg
*.egg-info/
.eggs/
eggs/
.env
env/
ENV/
env.bak/
# Environments
.envrc
# Exception: the pretraining caches are the only surviving copy of the Finding #3 data (their
/flash-attention/
# Flask stuff:
#   For a library or package, you might want to ignore these files since the code is
# (`git add -f`), as they were in commit 04d4980.
#   having no cross-platform support, pipenv may install dependencies that don't work, or not
.hf_cache/
# (HF_DATASETS_CACHE is repointed here per checkpoint; the run deletes its own on success).
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
htmlcov/
#   https://pdm-project.org/en/latest/usage/project/#working-with-version-control
#   https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
.hypothesis/
# icml_2026 dirs were missing these patterns, so `pdflatex` output showed up as untracked.
#.idea/
.idea/
# Ignore directories containing user credentials, local state, and settings.
#   install all needed dependencies.
.installed.cfg
# Installer logs
instance/
#   intended to run in multiple environments; otherwise, check them in:
#   in the .venv directory. It is recommended not to include this directory in version control.
.ipynb_checkpoints
# IPython
ipython_config.py
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
# Jupyter Notebook
# LaTeX build artifacts
# LaTeX build artifacts for the remaining manuscript directories. The neurips_2026 and
# Learn more at https://abstra.io/docs
lib/
lib64/
/lm-evaluation-harness/
local_settings.py
*.log
logs/
*.manifest
MANIFEST
manuscript/*.aux
manuscript/*.bbl
manuscript/*.blg
manuscript/*.fdb_latexmk
manuscript_fdgm_icml_2026/00_main.pdf
manuscript_fdgm_icml_2026/*.aux
manuscript_fdgm_icml_2026/*.bbl
manuscript_fdgm_icml_2026/*.blg
manuscript_fdgm_icml_2026/build_*.log
manuscript_fdgm_icml_2026/*.fdb_latexmk
manuscript_fdgm_icml_2026/*.fls
manuscript_fdgm_icml_2026/*.lof
manuscript_fdgm_icml_2026/*.lot
manuscript_fdgm_icml_2026/*.out
manuscript_fdgm_icml_2026/*.synctex.gz
manuscript_fdgm_icml_2026/*.toc
manuscript/*.fls
manuscript_icml_2026/00_main.pdf
manuscript_icml_2026/*.aux
manuscript_icml_2026/*.bbl
manuscript_icml_2026/*.blg
manuscript_icml_2026/*.out
manuscript_icml_2026/*.toc
manuscript/*.lof
manuscript/*.lot
manuscript_neurips_2026/00_main.pdf
manuscript_neurips_2026/*.aux
manuscript_neurips_2026/*.bbl
manuscript_neurips_2026/*.blg
manuscript_neurips_2026/*.out
manuscript_neurips_2026/*.toc
manuscript/*.out
manuscript/*.pdf
manuscript/*.synctex.gz
manuscript/*.toc
__marimo__/
# Marimo
marimo/_lsp/
marimo/_static/
# mkdocs documentation
*.mo
models/
# mypy
.mypy_cache/
nosetests.xml
!notebooks/04_pretraining_loss_scaling/data/43bce56c5b3db7fbdab964899d0f783e_runs_configs.parquet
notebooks/10_math_qwen3_pt_cross_entropy/data
notebooks/11_math_qwen3_pt_math_verify/data
notebooks/14_math_qwen3_pt_math_verify_teacher_forcing/data
!notebooks/**/data/c39ba9b590fe96b52183328d3d4c7323_runs_configs.parquet
notebooks/**/data/*.feather
notebooks/**/data/*.parquet
# notebooks/*/results/ and reviews/2026_neurips/data/.
.nox/
# only affects untracked paths.
# only the .csv left it unable to load from a clean checkout.
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
parts/
# pdm
.pdm-build/
#pdm.lock
.pdm-python
#   pdm recommends including project-wide configuration in pdm.toml, but excluding .pdm-python.
#pdm.toml
# PEP 582; used by e.g. github.com/David-OConnor/pyflow and github.com/pdm-project/pdm
# per-problem history caches out of the repo.
# Per-run tokenized dataset caches written by pretrain_language_model*.py
pip-delete-this-directory.txt
# pipenv
#Pipfile.lock
pip-log.txt
# pixi
.pixi
#   Pixi creates a virtual environment in the .pixi directory, just like venv module creates one
#pixi.lock
# poetry
#poetry.lock
#poetry.toml
*.pot
profile_default/
.pybuilder/
# PyBuilder
**/__pycache__/
__pycache__/
# PyCharm
*.py[codz]
*.py.cover
# pyenv
# PyInstaller
__pypackages__/
# PyPI configuration file
.pypirc
.pyre/
# Pyre type checker
.pytest_cache/
.Python
# .python-version
.pytype/
# pytype static type analyzer
# Raw harness output and scratch result dumps; the curated versions live in
# Raw per-problem generations from contaminant evals (5,000 rows x many checkpoints).
/results/
results/contaminant_eval*/*.jsonl
results/pass_at_k/**/*.jsonl
results/poscontrol_*/*.jsonl
.ropeproject
# Rope project settings
.ruff_cache/
# Ruff stuff:
.run/
# SageMath parsed files
*.sage.py
.scrapy
# Scrapy stuff:
# Scratch files from agent/delegate sessions
sdist/
share/python-wheels/
#   Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#   Similar to Pipfile.lock, it is generally recommended to include pixi.lock in version control.
#   Similar to Pipfile.lock, it is generally recommended to include poetry.lock in version control.
#   Similar to Pipfile.lock, it is generally recommended to include uv.lock in version control.
/site
*.so
*.spec
# Sphinx documentation
.spyderproject
# Spyder project settings
.spyproject
# src.analyze.download_wandb_project_runs_{configs,histories}. Ignoring them keeps ~12 GB of
/src/lm-eval-output/
# Streamlit
.streamlit/secrets.toml
# Summaries and derived tables are committed; the generations are reproducible.
target/
#  that can be found at https://github.com/github/gitignore/blob/main/Global/VisualStudioCode.gitignore
# the .feather/.parquet are format duplicates of the same download and are regenerable via
# The irreplaceable pretraining caches (c39ba9b5..., 43bce56c...) are tracked as .csv and are
# Third-party clones checked out in-tree by the setup in CLAUDE.md, not our code.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
.tmp_*
.tox/
# Translations
# unaffected -- see MISSING_PRETRAINING_DATA.md. Files already tracked stay tracked; .gitignore
# Unit test / coverage reports
#  Usually these files are written by a python script from a template
# UV
#uv.lock
var/
.venv
venv/
venv.bak/
# Visual Studio Code
#  Visual Studio Code specific template is maintained in a separate VisualStudioCode.gitignore 
# .vscode/
wandb/
# W&B download caches under notebooks/*/data/. Convention: the CSV is the committed copy;
# W&B project no longer resolves), and notebook 11 loads with filetype="parquet". Committing
.webassets-cache
wheels/
#  you could uncomment the following to ignore the entire vscode folder
data/hendrycks_math/*/*/cache-*.arrow
